import React, { useState } from "react";
import Button from "./components/Button";

const App = () => {
  return (
    <div>
      {/* default disable text hover */}
      <Button data={"hover"} />
    </div>
  );
};

export default App;
