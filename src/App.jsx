import React, { useState } from "react";
import Button from "./components/Button";

const App = () => {
  return (
    <div>
      {/* default disable text hover */}
      <Button  />
    </div>
  );
};

export default App;
